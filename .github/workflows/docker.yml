name: docker
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container: debian:buster
    steps:
    - uses: actions/checkout@v2
    - name: Release
      run: |
        .github/workflows/release.sh ci
